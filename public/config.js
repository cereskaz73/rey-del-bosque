// ÚNICO sitio que hay que tocar al desplegar el frontend por separado (GitHub Pages, Vercel, Netlify).
// Si el frontend lo sirve el propio servidor Node (opción recomendada), deja null: usará el mismo dominio.
// Si lo alojas aparte, pon aquí la URL pública del backend, p. ej.:
// window.BACKEND_URL = "https://rey-del-bosque.onrender.com";
window.BACKEND_URL = null;
